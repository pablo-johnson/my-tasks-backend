module.exports = {
  overrides: [
    {
      files: ['src/**/*.ts', 'src/**/*.tsx'], // Lint only TypeScript files
      rules: {
        // Your TypeScript-specific rules here
      },
    },
  ],
};